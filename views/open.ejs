<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&display=swap"
      rel="stylesheet"
    />
    <title>message</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        font-family: "Lexend";
        font-size: 18px;
      }
      a {
        text-decoration: none;
        color: black;
      }
      body {
        display: flex;
        flex-direction: column;
        height: 100vh;
        justify-content: center;
        align-items: center;
      }
      div {
        display: flex;
        flex-direction: column;
        width: 70%;
        gap: 8px;
        padding: 15px;
        border: 1px groove black;
        border-radius: 4px;
      }
      button {
        cursor: pointer;
        padding: 9px;
        border: 0;
        background-color: rgba(0, 0, 0, 0.274);
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    <div>
      <p class="element">User: <%= message[0]["username"] %></p>
      <p class="element">Text: <%= message[0]["message"] %></p>
      <p class="element">Added: <%= message[0]["added"] %></p>
      <button class="element"><a href="/">Go back</a></button>
    </div>
  </body>
</html>
